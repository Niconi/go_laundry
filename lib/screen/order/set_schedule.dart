import 'package:flutter/material.dart';

class SetSchedule extends StatelessWidget {
  const SetSchedule({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This Is Set Scheddule Screen'),
      ),
    );
  }
}
